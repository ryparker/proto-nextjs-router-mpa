import IconButton from './IconButton';

export default {
  component: IconButton,
  title: 'Draft 1/IconButton',
};

export const Default = (args: any) => <IconButton {...args} />;
Default.args = {};
